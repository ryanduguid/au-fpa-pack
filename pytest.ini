[pytest]
testpaths = tests
python_files = test_*.py
addopts = -q -m "not network"
markers =
    network: hits a live third-party endpoint. Excluded from the merge gate so a
        publisher outage or layout change cannot fail an unrelated PR. Run the
        live checks deliberately with: pytest -m network
